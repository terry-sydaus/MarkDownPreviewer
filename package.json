{
  "name": "markdownpreviewer",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "marked": "^0.6.2",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "2.1.8",
    "react-text-transition": "^0.1.8"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "gh-pages -d build",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "homepage": "https://terry-sydaus.github.io/MarkDownPreviewer",
  "devDependencies": {
    "gh-pages": "^2.0.1"
  }
}
